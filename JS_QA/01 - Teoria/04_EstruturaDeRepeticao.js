const cidades = ["SP", "BA", "MG",]

cidades.forEach((elemento, index) => {
    console.log("Execução número: " + index + " = " + elemento)
    //console.log(elemento)
})

for (let index1=0; index1 < 10; index1++)
{
    const.elemento = array[index1]
}